{
  "buildCommand": "npm install:all && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "env": {
    "PROVIDER": "openai",
    "MODEL": "gpt-5.4"
  }
}
